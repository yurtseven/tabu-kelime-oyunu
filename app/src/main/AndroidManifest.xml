<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupkobe.tabu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Kobeee"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ayarlar" />
        <activity android:name=".takim_secme" />
        <activity android:name=".oyun_sayfasi" />
        <activity android:name=".admin_paneli" />
        <activity android:name=".kelime_ekleme" />
        <activity android:name=".oyun_sayfasi2" />
        <activity android:name=".skor" />
        <activity android:name=".skor2" />
        <activity android:name=".hakkimizda"></activity>
    </application>

</manifest>